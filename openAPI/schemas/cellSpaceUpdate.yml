CellSpaceUpdate:
      type: object
      title: CellSpaceUpdate
      description: "Available fields for partial update. All fields are optional."
      properties:
        cellSpaceName:
          type: string
          example: "Conference Room A"
        poi:
          type: string
          example: "Restroom"
        boundedBy:
          type: array
          items: 
            type: string
          example: ["B1"]
example:
  $ref: '../examples/cellSpaceUpdate.json'