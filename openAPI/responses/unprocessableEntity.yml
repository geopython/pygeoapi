description: Unprocessable Entity (Missing Required field for schema or logical mismatch).
content:
  application/json:
    schema:
      $ref: '../schemas/ApiError.yml'
    example:
      $ref: '../examples/unprocessableEntity.json'