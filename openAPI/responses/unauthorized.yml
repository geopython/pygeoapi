description: Authentication credentials were missing or invalid.
content:
  application/json:
    schema:
      $ref: '../schemas/apiError.yml'
    example:
      $ref: '../examples/unauthorizedError.json'