hop:
      name: hop
      in: query
      description: Maximum hop distance from the source node.
      required: true
      schema:
        type: integer
        minimum: 0
        maximum: 4
        default: 1
      style: form
      explode: false