apiVersion: v1
kind: Service
metadata:
  name: {{ include "pygeoapi-helm.fullname" . }}
  labels:
    {{- include "pygeoapi-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pygeoapi-helm.selectorLabels" . | nindent 4 }}
